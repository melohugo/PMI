name: Atualização Automática do Ranking

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  build-and-update-ranking:
    runs-on: ubuntu-latest

    steps:
      - name: 1. Checkout do Repositório
        uses: actions/checkout@v3

      - name: 2. Configurar o Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: 3. Instalar o Navegador Firefox
        uses: moz-actions/setup-firefox@v4
        with:
          firefox-version: 'latest'

      - name: 4. Instalar o Navegador Firefox
        run: |
          sudo apt-get update
          sudo apt-get install -y firefox

      - name: 5. Instalar Dependências Python
        run: |
          python -m pip install --upgrade pip
          pip install selenium webdriver-manager python-dotenv rich tqdm

      - name: 6. Executar o Script de Web Scraping
        env:
          USUARIO: ${{ secrets.EDPUZZLE_USUARIO }}
          SENHA: ${{ secrets.EDPUZZLE_SENHA }}
        run: |
          python scripts/importa_dados.py

      - name: 7. Exibir logs do GeckoDriver em caso de falha
        if: failure()
        run: |
          echo "--- Exibindo conteúdo do geckodriver.log ---"
          cat geckodriver.log || echo "geckodriver.log não encontrado."
          echo "--- Fim do log ---"

      - name: 8. Fazer Commit e Push das Alterações
        if: success()
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add ranking.json
          git diff --staged --quiet || git commit -m "📊 Atualização automática do ranking"
          git push
